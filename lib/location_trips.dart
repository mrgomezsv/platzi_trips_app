import 'package:flutter/material.dart';

class LocationStrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: Colors.orange,
    );
  }

}